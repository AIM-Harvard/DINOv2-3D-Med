system#model#backbone:
  _target_: project.models.masked_vit_wrapper.MaskedVisionTransformerMONAI3D
  vit:
    _target_: monai.networks.nets.vit.ViT # Vit-S/4 
    in_channels: 1
    img_size: "%img_size"
    patch_size: "%patch_size"
    hidden_size: 768
    mlp_dim: 3072
    num_layers: 8
    num_heads: 12
    proj_type: "conv"
    classification: True